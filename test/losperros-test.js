const assert = require('chai').assert
const expect = chai.expect;

const { dogsWeLove } = require('../data/losperros')

describe('PROTOTYPES', () =>{
  it('Should create an array of all dogs that know how to fetch', () =>{
    
  }), 

    it('create an array of objects where each object has the dogs name and dogs age updated to dog years', () => {

  })
})
